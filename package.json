{
  "name": "tachin",
  "private": true,
  "version": "1.0.0",
  "description": "Web de Tachín + panel admin (/admin) para gestionar el catálogo de vinos, listo para desplegar en Vercel.",
  "scripts": {
    "dev": "vercel dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "@vercel/edge": "^1.3.1"
  }
}
